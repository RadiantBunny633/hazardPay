# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/hazardpay

# For MongoDB Atlas (cloud - recommended):
# MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/hazardpay?retryWrites=true&w=majority

# Database name
DB_NAME=hazardpay

# Scraping Configuration
SCRAPE_DELAY=2.0
REQUEST_TIMEOUT=30

# Default platform: 'ps' or 'pc'
DEFAULT_PLATFORM=ps
